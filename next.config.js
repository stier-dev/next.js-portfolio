/**
 * @type {import('next').NextConfig}
 */

/** @type {import('next').NextConfig} */
const nextConfig = {
  // reactStrictMode: true,
  // concurrentFeatures: true,
  // swcMinify: true,
};

module.exports = nextConfig;
