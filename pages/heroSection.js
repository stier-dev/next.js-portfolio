import styles from "@/styles/HeroSection.module.scss";

export default function HeroSection() {
  return (
    <div className={styles.mainContainer} id="heroSection">
      <h1>I am a Matrix Header</h1>
    </div>
  );
}
